dependencies:
  pre:
    - pip install -r build/pipreq.txt
test:
  override:
    - ./manage.py test aor --noinput
deployment:
  production:
    branch: master
    commands:
      - ssh zeus@${DEPLOY_HOST} /home/zeus/deploy.sh aor production ${ANSIBLE_PASSWORD}:
          timeout: 600
