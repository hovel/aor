dependencies:
  pre:
    - pip install -r build/pipreq.txt -U --allow-unverified postmarkup
test:
  override:
    - ./manage.py test aor --noinput
deployment:
  production:
    branch: master
    commands:
      - fab update
